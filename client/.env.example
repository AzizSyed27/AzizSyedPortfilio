# Copy to .env.local and fill with values from your EmailJS dashboard.
# All three are required for the contact form to send.
# Public key is intentionally public — EmailJS treats it as an identifier, not a secret.

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
